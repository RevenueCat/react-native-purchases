/**
 * @file Restore Purchases Button styles.
 * @author Vadim Savin
 */

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 'auto',
  },
  text: {
    color: 'dodgerblue',
    fontFamily: 'ArialRoundedMTBold',
    fontSize: 18,
    paddingVertical: 16,
  },
});

export default styles;
