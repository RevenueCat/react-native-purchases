/**
 * @file Logout Button styles.
 * @author Vadim Savin
 */

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 'auto',
  },
  text: {
    color: 'red',
    fontFamily: 'ArialRoundedMTBold',
    fontSize: 18,
  },
});

export default styles;
